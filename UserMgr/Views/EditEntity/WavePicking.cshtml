@using UserMgr.Models
@model WavePickingViewModel

@{
    ViewBag.Title = "修改波次信息";
    Layout = "~/Views/Layout/_LayoutIndex.cshtml";
}


<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-sm-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>添加波次</h5>
                </div>
                <div class="ibox-content">
                    @using (Html.BeginForm("WavePicking", "EditEntity", FormMethod.Post, new { @class = "form-horizontal m-wp" }))
                    {
                        @Html.HiddenFor(wp => wp.WavePickingID)
                        <div class="form-group">
                            <label class="col-sm-3 control-label">波次编码：</label>
                            <div class="col-sm-8">
                                @Html.TextBoxFor(wp => wp.WavePickingNo, new { @class = "form-control", @readonly = "" })
                                @Html.ValidationMessage("WavePickingNo", new { @class = "text-danger pull-right" })
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-3 control-label">波次类型：</label>
                            <div class="col-sm-3">
                                @Html.DropDownListFor(wp => wp.WavePickingTypeID, ViewData["WavePickingType"] as IEnumerable<SelectListItem>, new { @class = "form-control", @style = "font-size:13px" })
                                @Html.ValidationMessage("WavePickingTypeID", new { @class = "text-danger pull-right" })
                            </div>
                            <label class="col-sm-2 control-label">拣货类型：</label>
                            <div class="col-sm-3">
                                @Html.DropDownListFor(wp => wp.PickingType, ViewData["PickingType"] as IEnumerable<SelectListItem>, new { @class = "form-control", @style = "font-size:13px" })
                                @Html.ValidationMessage("PickingType", new { @class = "text-danger pull-right" })
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-3 control-label">状态：</label>
                            <div class="col-sm-8">
                                @Html.DropDownListFor(wp => wp.Status, ViewData["Status"] as IEnumerable<SelectListItem>, new { @class = "form-control", @style = "font-size:13px" })
                                @Html.ValidationMessage("Status", new { @class = "text-danger pull-right" })
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-3 control-label">备注说明：</label>
                            <div class="col-sm-8">
                                @Html.TextAreaFor(wp => wp.Remark, new { @class = "form-control" })
                                @Html.ValidationMessage("Remark", new { @class = "text-danger pull-right" })
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-sm-4 col-sm-offset-3">
                                <button class="btn btn-primary" type="submit">修改</button>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>




